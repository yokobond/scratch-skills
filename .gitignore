# Temporary test artifacts — created by scratch-skill-tester
tmp/

# Playwright MCP temporary files
.playwright-mcp/

# Installed agent skills (apm install, gh skill install)
.claude/skills/
.github/skills/
.cursor/skills/
